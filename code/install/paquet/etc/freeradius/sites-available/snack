server snack {
	authorize {
		preprocess
		suffix
		expiration
		logintime

		eap {
			ok = return
		}
		sql

		chap
		pap
	}

	authenticate {
		eap

		Auth-Type CHAP {
			reject
		}

		Auth-Type PAP {
			pap
		}
	}

	preacct {
		preprocess
		acct_unique
		suffix
	}

	accounting {
		detail
		radutmp
		sql
	}

	session {
		radutmp
		sql
	}

	post-auth {
		sql

		Post-Auth-Type REJECT {
			attr_filter.access_reject
		}
	}

	pre-proxy {
	}

	post-proxy {
		eap
	}
}
